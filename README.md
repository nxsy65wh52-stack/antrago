# ANTRAGO Next.js App (Placeholder)

This folder contains a minimal Next.js App Router + TypeScript scaffold and a single page at `/app/page.tsx` implementing a small chat UI (no AI integrated).

Run locally:

1. cd next-app
2. npm install
3. npm run dev
